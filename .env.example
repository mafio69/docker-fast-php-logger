GIT_ACCES_TOKEN=

MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_FROM=dev@localhost

# Ports (host:container)
PHP_PORT=8794
DB_PORT=3307
MAILPIT_SMTP_PORT=1025
PROXY_PORT=80

# Database credentials (used by docker-compose)
DB_DATABASE=devbox
DB_USERNAME=devbox
DB_PASSWORD=
MYSQL_ROOT_PASSWORD=

# SSH test endpoint auth token (leave empty to disable the endpoint)
SSH_TEST_TOKEN=
