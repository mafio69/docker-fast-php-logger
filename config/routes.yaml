# yaml-language-server: $schema=../vendor/symfony/routing/Loader/schema/routing.schema.json

# Load controller routes from PHP 8 attributes
# See https://symfony.com/doc/current/routing.html

controllers:
    resource: ../src/Controller/
    type: attribute
